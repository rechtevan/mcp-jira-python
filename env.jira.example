# Jira MCP Server Configuration
# Copy this file to .env.jira and fill in your values
# .env.jira is gitignored so your secrets stay local

# Required: Your Jira server URL
# Can be hostname only (assumes https://) or full URL
JIRA_HOST=jira.yourcompany.com
# JIRA_HOST=https://jira.yourcompany.com/jira

# === Authentication (choose one) ===

# Option 1: Bearer Token (Jira Server/Data Center 8.14+)
# Use a Personal Access Token (PAT)
JIRA_BEARER_TOKEN=your_pat_token_here

# Option 2: Basic Auth (Jira Cloud)
# JIRA_EMAIL=you@company.com
# JIRA_API_TOKEN=your_api_token_here

